import { defineConfig } from "https://esm.sh/windicss/helpers";

export default defineConfig({
    theme: {
        extend: {
            fontFamily: {
                sans: ['"IBM Plex Sans"', "sans-serif"],
            },
        },
    },
});
